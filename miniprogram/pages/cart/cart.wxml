<view class="main">

    <view wx:if="{{orderList}}">
        <checkbox-group class="cart-box">

            <view class="cart-item" wx:for="{{orderList}}" wx:key="{{index}}">
                <checkbox></checkbox>
                <navigator url="../details/details?id={{item._id}}"><image class="room-img" src="{{item.roomImg}}" mode='widthFix'></image></navigator>
                <view class='order-info'>
                    <text>[{{item.quantity}}]间 </text><text class="room-type"> {{item.roomType}}</text>
                    <view class='date'>{{item.checkInDate}} 至 {{item.checkOutDate}}</view>
                    <view>宿 [{{item.stay}}]晚</view>
                    <view>总价：<text class='price'>RMB {{item.total}}</text></view>
                    <view>{{item.forWho}}</view>
                </view>
                <!-- <text class="cart-pro-price">￥{{item.total}}</text> -->
               
                <!-- <text class="cart-del" bindtap="deleteList" data-index="{{index}}">×</text> -->
            </view>

        </checkbox-group>

        <view class="cart-footer">
            <icon wx:if="{{selectAllStatus}}" type="success_circle" color="#fff" class="total-select" bindtap="selectAll"/>
            <icon wx:else type="circle" color="#fff" class="total-select" bindtap="selectAll"/>
            <view class="order-icon">
                <view bindtap="saveOder" ><image src="/lib/images/icon3.png"></image></view>
            </view>
            <text>全选</text>
            <text class="cart-toatl-price">￥{{totalPrice}}</text>
        </view>
    </view>


    <view wx:else>
        <view class="cart-no-data">购物车是空的哦~</view>
    </view>
</view>